# ────────────────────────────────────────────────────────────────────────────────
# CEENI PLATFORM – LOCAL ENVIRONMENT VARIABLES
# This file is for local development only. Do not commit this file to Git.
# It is loaded by dotenv in dev.py.
# ────────────────────────────────────────────────────────────────────────────────

# Django environment mode
DJANGO_ENV=dev                      # Can be 'dev' or 'prod'

# Security
SECRET_KEY=s3cr3t-k3y-f0r-dev-env  # Django secret key for cryptographic signing
DEBUG=True                          # Enables debug mode (set to False in prod)

# PostgreSQL Database Settings
DB_NAME=ceeni_platform_db           # Name of your local dev database
DB_USER=ceeni_platform_user         # Database user
DB_PASSWORD=@0BusiaKenya            # Database password
DB_HOST=localhost                   # DB host (default: localhost)
DB_PORT=5432                        # DB port (default for PostgreSQL)
