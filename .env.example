# ────────────────────────────────────────────────────────────────────────────────
# CEENI PLATFORM – ENV EXAMPLE TEMPLATE
# This file should be committed. It documents the expected environment variables.
# Copy this to `.env` and fill in actual values.
# ────────────────────────────────────────────────────────────────────────────────

# Django environment mode
DJANGO_ENV=dev                      # dev or prod

# Security
SECRET_KEY=your-secret-key          # Use a long, random string in production
DEBUG=True                          # True for dev, False for production

# PostgreSQL Database Settings
DB_NAME=ceeni_platform_db           # Name of your PostgreSQL DB
DB_USER=ceeni_platform_user         # Database username
DB_PASSWORD=your-password           # Database password (keep secure)
DB_HOST=localhost                   # Usually 'localhost' or a container name
DB_PORT=5432                        # PostgreSQL port (default is 5432)
